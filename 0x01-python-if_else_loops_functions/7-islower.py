#!/usr/bin/python3
# islower - function that checks for lowercase character
# Return - True if c is lowercase otherwise, False
def islower(c):
    if 123 > ord(c) > 96:
        return True
    else:
        False
